# Scaiences — Claude Code Instructions

## Editing files

- Always Read a file immediately before editing it, even if it was read earlier in the conversation. The user edits files in the IDE between turns.
- Never use Write on a file that already exists. Write overwrites the entire file and destroys the user's changes. Use Edit for existing files. Write is only for creating new files.
